h3 Add comment
form#comment-form(name= "" + pieceId)
    textarea.form-control(name="commentText")
    button.btn.btn-default(type="submit") Comment
h3 Comments
for c in comments
    .comment
        h4 
            a.view-profile(href="#", name=c.writer) #{c.writer}
        .text #{c.text}

